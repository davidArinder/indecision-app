import React from 'react'
import ReactDOM from 'react-dom'
import IndecisionApp from './components/IndecisionApp' // imported as default so no curly braces

ReactDOM.render(<IndecisionApp />, document.getElementById('app'))